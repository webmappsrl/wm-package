> Ticket: oc:8367

# Notes — Theme colori app — configurazione da backend

## Deviazioni dal piano

- **Esecuzione dei test Pest impossibile in locale**: `composer install` su `wm-package` in questo ambiente Docker fallisce perché richiede `laravel/nova` (pacchetto privato) — il download da dist riceve `403`, il fallback su clone Git SSH fallisce (`Host key verification failed`, nessuna chiave SSH con accesso al repo privato in questo container). Verificato che `vendor/composer/installed.json` non esiste: `wm-package` non ha mai avuto un install Composer completo in questo ambiente. Corrisponde a un limite già documentato in `wm-package/CLAUDE.md` (oc:7546, "credenziali nova.laravel.com scadute in questo ambiente"), non introdotto da questo ciclo.
  - I test Pest (`AppConfigServiceThemeTest.php`, `AppConfigServiceMapFeatureCollectionColorTest.php`) sono stati scritti secondo il piano ma **non eseguiti localmente** — la verifica del ciclo TDD (write-fail → implement → pass) è stata sostituita da un controllo equivalente via `php artisan tinker` sul container `php-camminiditalia`: invocazione diretta di `config_section_theme()` tramite reflection su istanze `App` non persistite, con i 5 scenari esatti dei test Pest (tutte le 9 chiavi popolate, valori vuoti esclusi, `properties->theme` assente, `properties->theme` malformato, replica del dato reale camminiditalia) — tutti e 5 gli output hanno corrisposto esattamente alle asserzioni scritte nei test.
  - Il fix di `config_section_map()` (Task 3) **non** ha una verifica equivalente via tinker sui dati reali: avrebbe richiesto creare una riga `FeatureCollection` di test nel DB di sviluppo reale (l'unica App di camminiditalia), scartato per prudenza (nessuna scrittura di dati non necessaria sul DB reale). La correttezza si basa sul pattern identico già verificato e funzionante in `StoryShareImageService::resolveAccentColor()` (stessa lettura `properties['theme']['primary_color'] ?? fallback`) e sulla verifica di sintassi PHP (`php -l`).
  - **Verifica effettiva rimandata alla CI — smentito nella seconda passata di review**: verificato che `.github/workflows/run-tests.yml`/`phpstan.yml` triggerano solo su PR verso `main`/`develop`, non verso `RDO_ass_cammini_italia_2026_2` (il branch di integrazione corretto per questo ciclo, confermato dal dev) — la CI **non scatterà** su questa PR. Nessuna rete di sicurezza automatizzata disponibile per questo branch; dettaglio completo nella sezione "Review — round 2" più sotto.

## Bug trovati

- **`config_section_map()` (`AppConfigService.php:500`)**: il fallback colore per i box "feature_collection" della home leggeva `$this->app->primary_color`, una colonna DB reale della tabella `apps` (definita in `create_apps_table.php.stub`, default `#de1b0d`) mai scritta da Nova — che scrive invece su `properties->theme->primary_color` (JSON). La colonna era quindi sempre al valore di default della migration, indipendentemente dal colore primario reale impostato dall'admin. Trovato durante la Fase: write-plan di questo ciclo (non nel ticket originale), corretto nello stesso ciclo su richiesta esplicita del dev — stessa causa radice del bug principale (storage reale disconnesso dal JSON `theme`).
- Le colonne DB morte (`font_family_header`, `font_family_content`, `default_feature_color`, `primary_color` sulla tabella `apps`) non sono state rimosse — restano orfane ma inerti. Nessun altro consumer trovato oltre a quello corretto in questo ciclo (verificato con grep su tutto `wm-package` e sul repo principale camminiditalia).

## Decisioni

- Nessuna migrazione dati: la traduzione snake_case → camelCase avviene solo nel layer di output (`config_section_theme()`), i dati già salvati sotto `properties->theme->*` restano intatti (verificato sul dato reale camminiditalia: `primary_color`/`default_feature_color` = `#ef7821`).
- Regex di validazione color picker (`^#([A-Fa-f0-9]{3}|[A-Fa-f0-9]{6})$`) accetta hex a 3 cifre solo per tolleranza verso valori scritti via API/tinker — il campo Nova `Color` (`<input type="color">` nativo) non produce mai un valore a 3 cifre dal picker stesso.
- Ruoli `dark`/`medium`/`light`, `select` e le 7 chiavi font-size di `ITHEME` restano fuori scope — nessun campo Nova aggiunto, restano ai default del frontend.
- Layout dei campi Nova resta flat nel tab "Theme" (nessun Panel di raggruppamento), su scelta esplicita del dev.

## Decisione post-commit: rimozione di success/warning/danger

`success_color`/`warning_color`/`danger_color` erano stati aggiunti a `theme_tab()` e `THEME_KEY_MAP` nei primi due commit del ciclo, poi **rimossi nello stesso ciclo** (nel terzo commit) su decisione esplicita del dev, in seguito a una verifica end-to-end eseguita da una sessione Claude parallela sul repo `webmapp-app` (frontend).

> **Nota su hash commit citati in questo file**: il branch è stato rebasato su `origin/RDO_ass_cammini_italia_2026_2` aggiornato dopo la stesura delle sezioni "Review" sotto — gli hash `37c19cac`/`60a30cce` citati non sono più raggiungibili da `HEAD` (sostituiti da `881617a6`/`85c233e2`, stesso contenuto). Riferimento storico, non usabile per `git show`/`git log` su un clone pulito dopo un eventuale `git gc`.

**Nota**: su richiesta esplicita del dev, `plan.md` è stato riallineato a posteriori (Task 1-4 aggiornati con blocchi "Aggiornato post-commit") per riflettere lo stato finale del codice, invece di restare il piano originale immutato — deviazione dalla convenzione di default di questo progetto (piano originale intoccato, deviazioni solo in `notes.md`).

- **Dato misurato, non dedotto**: conteggio dei consumatori reali `var(--wm-color-X)` in scss/css/html del frontend (esclusi `theme/variables.scss` e i `.md` di documentazione): `success` 0, `warning` 0, `danger` 4 (tutti usi semantici — bordi di errore validazione, indicatore filtri attivi). Impostare `success`/`warning` in Nova non produceva **nessun** effetto visibile nell'app oggi.
- **Prova aggiuntiva**: i valori già presenti nel DB camminiditalia per questi ruoli (`success: #f5f5f5`, grigio chiarissimo; `tertiary: #f6eade`, crema — impostati per test da terzi, non da questa sessione) confermano che venivano usati come "slot brand di scorta", non come colori semantici — se in futuro si cablasse `success` a un vero indicatore di successo, sarebbe grigio su bianco.
- **Raccomandazione della sessione frontend, non applicata in questo ciclo**: se il cliente ha bisogno di più slot per il brand, la scelta corretta sarebbe esporre `light`/`dark` (50/63 consumatori reali) — non riciclare ruoli semantici. Richiede la stessa cura UX già segnalata per `dark`/`medium`/`light` (vedi Decisioni sopra) — non affrontato in questo ciclo, resta fuori scope.
- **`tertiary` mantenuto**: pur avendo solo 3 consumatori reali, non è un ruolo semantico (non ha un significato "riservato" come success/danger) — nessun rischio di collisione di significato segnalato dalla sessione frontend, quindi resta in scope.
- **`default_feature_color` — inizialmente mantenuto, poi rimosso su richiesta esplicita del dev (vedi sezione dedicata più sotto)**: la sessione frontend aveva confermato che `map-core` non legge `THEME.defaultFeatureColor` (usa una costante hardcoded `DEF_LINE_COLOR = 'red'`). In un primo momento il campo era stato lasciato perché pre-esistente a oc:8367, non introdotto da questo ciclo — decisione poi rovesciata per coerenza con la rimozione di success/warning/danger (stesso sintomo, stesso trattamento).
- File aggiornati per la rimozione: `src/Nova/App.php` (`theme_tab()`, da 9 a 6 campi), `src/Services/Models/App/AppConfigService.php` (`THEME_KEY_MAP`, da 9 a 6 voci), `resources/lang/{it,en}.json` (rimosse le 6 chiavi di traduzione per i 3 campi), `tests/Feature/AppConfigServiceThemeTest.php` (aggiornato il test esaustivo).

- **Bypass PHPStan confermato dal dev** (2026-08-25T10:57:41+0000): `vendor/bin/phpstan` assente in questo ambiente (stessa causa root del blocco Pest — `composer install` bloccato da credenziali `laravel/nova` scadute, oc:7546). Motivazione: fallimento infrastrutturale, non errori di codice; la CI di wm-package (con credenziali valide) eseguirà PHPStan al push. Bypass confermato esplicitamente da Rubens Garofalo (dev) — responsabilità della verifica PHPStan post-push attribuita a lui.

## Review (`wm-review-ticket`, post-commit `37c19cac`)

5 finder paralleli + verifica diretta di ogni claim prima di accettarlo. Un bloccante reale confermato e corretto, uno confutato con evidenza concreta:

- **Bloccante trovato e corretto**: il fix di `config_section_map()` (riga 500) passava `properties['theme']['primary_color']` a `hexToRgba()` senza validarne il formato. Verificato direttamente (`hexToRgba('#de1')` lancia `Exception: Invalid hex color format.`) — un valore a 3 cifre, esplicitamente ammesso dalla regex Nova appena introdotta su questo stesso campo, avrebbe fatto fallire con 500 **qualsiasi** salvataggio dell'App. Corretto aggiungendo la stessa validazione stretta (6 cifre esatte) già usata in `StoryShareImageService::resolveAccentColor()`, con fallback a `#000000` se il formato non è valido. Verificato isolando la logica di guardia contro l'implementazione reale di `hexToRgba()` (letta riga per riga): 7 casi (incluso il trigger originale `#de1`, `null`, stringa vuota, formato non valido, 2 valori validi) tutti corretti.
- **"Finding confutato" della prima passata — ERRATO, corretto nella seconda passata**: nella prima review avevo respinto il finding su `use Tests\TestCase;` (namespace potenzialmente inesistente) basandomi solo sul fatto che 41 file pre-esistenti usano lo stesso pattern — un ragionamento fallace (la popolarità di un pattern non ne implica la correttezza). Un finder della seconda passata ha citato una prova diretta e verificabile: `wm-package/CLAUDE.md` (sezione oc:8183) documenta esplicitamente che `vendor/bin/pest` senza filtro **fallisce già oggi** su 19 file preesistenti che importano lo stesso `Tests\TestCase` inesistente, e che i test di oc:8183 hanno deliberatamente evitato questo "pattern rotto" per lo stesso motivo. Verificato di persona su `composer.json` (`autoload-dev` mappa solo `Wm\WmPackage\Tests\` → `tests/`, nessun mapping per `Tests\`) e su `tests/Pest.php` (bind di default su `Wm\WmPackage\Tests\TestCase`, non `Tests\TestCase`). **Corretto**: entrambi i nuovi file di test ora importano `Wm\WmPackage\Tests\TestCase` (namespace reale, verificato via lettura diretta di `tests/TestCase.php`), non `Tests\TestCase`.
- **Segnalazione branch-base non applicata**: un finder ha proposto che il branch fosse basato sul parent sbagliato (`RDO_ass_cammini_italia_2026_2` invece di `develop`). Il dev ha confermato esplicitamente che `RDO_ass_cammini_italia_2026_2` è il branch di integrazione corretto per questo ciclo di ticket — nessuna azione necessaria.
- **Finding cleanup applicati**:
  - `AppConfigService::config_section_theme()` riscritto con costante privata `THEME_KEY_MAP` (tabella esplicita snake_case→camelCase) iterata in loop, invece di 9 blocchi `if(!empty())` ripetuti — aggiunto anche un guard `is_string()` che esclude valori non scalari (es. un array scritto per errore in `properties->theme->*`) dall'output pubblico invece di lasciarli passare tale e quali (verificato con un nuovo test dedicato).
  - `Nova\App::theme_tab()` refactored con helper privato `themeColorField()` — i 7 color picker non ripetono più la stessa catena `->rules()->hideFromIndex()->help()`. Verificato via reflection che i 9 campi prodotti (label, attribute path, regex) sono identici a prima del refactor.
  - `config_section_map()`: il calcolo di `$primaryColor` (validazione + fallback) è stato spostato fuori dal loop `foreach ($overlayItems as $item)` — prima veniva ricalcolato ad ogni iterazione pur essendo costante per tutta la richiesta.
  - Aggiunto test di regressione per il bloccante corretto (hex a 3 cifre non causa più eccezione, fallback a `#000000`) e per il guard `is_string()`.
  - `overview.md` "Moduli toccati" aggiornato con l'elenco completo del diff reale (CLAUDE.md, notes.md/plan.md, secondo file di test).
- **Finding cleanup non applicati in questo ciclo** (non bloccanti, valutati e scartati con motivazione, non per pigrizia):
  - Un accessor condiviso per "leggi `properties->theme->primary_color` con fallback" tra `config_section_theme()`, `config_section_map()` e `StoryShareImageService::resolveAccentColor()` **non è stato creato**: le tre letture hanno semantiche di validazione genuinamente diverse (`config_section_theme()` tollera 3 o 6 cifre hex — vs validazione stretta a 6 cifre necessaria prima di `hexToRgba()` nelle altre due). Un accessor unico avrebbe forzato una delle due semantiche sull'altra, introducendo una regressione o un falso positivo. **Aggiornato in round 3**: `config_section_theme()` non era più "permissiva by design" come descritto qui — vedi sezione "Review — round 3" più sotto, il gap è stato chiuso con una validazione dedicata (3-o-6 cifre, coerente con la regex Nova), non lasciato come passthrough libero.
  - Regex di validazione retroattiva su `primary_color`/`default_feature_color` (che potrebbe bloccare con 422 il salvataggio su altre istanze wm-package con valori storici non conformi) non è stata rimossa — è un trade-off consapevole già discusso in Fase: challenge, non un cleanup da applicare.

## Review — round 2 (`wm-review-ticket`, dopo il fix del bloccante round 1 e i cleanup)

5 nuovi finder paralleli sul diff aggiornato. Trovati e corretti **2 bloccanti reali** (uno dei quali era stato erroneamente respinto nel round 1 — vedi sopra), più un dedup cross-file applicato:

- **Bloccante trovato e corretto — stesso bug, metà strada**: il fix del round 1 validava solo `$primaryColor` prima di `hexToRgba()` in `config_section_map()`, ma **due righe sotto**, nella stessa funzione appena toccata, `$fc->fill_color`/`$fc->stroke_color` (campi Nova `Text::make()->nullable()` **senza alcuna validazione**, `src/Nova/FeatureCollection.php`) venivano passati a `hexToRgba()` altrettanto senza guardia — stessa identica classe di bug, non corretta. Confermato in triplice modo (2 finder indipendenti + verifica diretta: `hexToRgba('#fff')` e `hexToRgba('red')` — il secondo non lancia perché senza `#` passa attraverso invariato, ma `#fff`/`#de1`/qualsiasi hex non a 6/8 cifre sì). **Aggravante — corretto nella terza passata di review, era impreciso**: raggiungibile non solo al salvataggio Nova ma anche da un endpoint pubblico non autenticato — ma **solo** `AppController::baseConfig()` (`/{app}/base-config.json`), che chiama `(new AppConfigService($app))->writeAppConfigOnAws()` sincronamente senza try/catch né cache, ad ogni richiesta. **Non** `AppController::config()` (`/{app}/config.json`): quella rotta legge prima da `StorageService::getAppConfigJson()` (JSON già pubblicato su S3) e ricalcola live solo in caso di cache-miss, chiamando `$app->BuildConfJson($app->id)` — un metodo che **non esiste** in nessun punto di `wm-package` (bug preesistente e indipendente, non toccato da questo ticket): quel path fallirebbe comunque con un errore diverso, prima ancora di raggiungere `config_section_map()`. Verificato leggendo `src/Http/Controllers/Api/AppController.php:374-386`. Corretto estraendo la validazione in una funzione globale condivisa `sanitizeHexColor()` (`src/helpers.php`, accanto a `hexToRgba()`) e applicandola a tutti e 3 i valori (`$primaryColor`, `$fc->fill_color`, `$fc->stroke_color`). Aggiunti 2 test di regressione (fallback su `fill_color`/`stroke_color` malformati, uso dei valori validi propri della FeatureCollection quando presenti).
- **Dedup cross-file applicato**: `StoryShareImageService::resolveAccentColor()` duplicava la stessa identica regex/logica di validazione (stesso guard, solo fallback diverso: `#000000` vs `#FFFFFF`) — non una semantica genuinamente diversa come avevo argomentato nel round 1 per `config_section_theme()` (quella resta permissiva by design, per il passthrough CSS-facing). Estratta la funzione globale `sanitizeHexColor($value, $fallback)` in `src/helpers.php`, usata ora da entrambi i punti. Verificato via reflection che `resolveAccentColor()` produce lo stesso output di prima su input validi/malformati.
- **Finding cleanup non applicati** (valutati, motivazione esplicita):
  - Nessun test automatizzato che verifichi la corrispondenza 1:1 tra gli attribute path di `theme_tab()` e le chiavi di `THEME_KEY_MAP` (oggi solo un commento "mantenere sincronizzati") — segnalato come rischio di drift silenzioso reale (stesso pattern del baseline PHPStan, oc:8312), ma non aggiunto in questo ciclo per contenere lo scope del secondo giro di review; da valutare come follow-up.
  - Testo di `overview.md`/`notes.md` sulla motivazione della tolleranza a 3 cifre in `config_section_theme()` descritta come "passthrough di CSS valido" mentre il codice reale accetta qualunque stringa non vuota (non solo hex a 3/6 cifre) — imprecisione di documentazione, non un bug: la UI Nova impedisce comunque input non hex, il gap esiste solo per scritture via API/tinker già esplicitamente tollerate per design. **Riclassificato in round 3**: quella che il round 2 ha liquidato come "solo imprecisione di documentazione" era in realtà un gap di codice reale — vedi sotto.

## Review — round 3 (`wm-review-ticket`, dopo la rimozione di success/warning/danger)

5 nuovi finder paralleli. Nessun bloccante in senso stretto (nessuna eccezione/500 introdotta), ma un finder ha correttamente riaperto un punto che il round 2 aveva chiuso troppo in fretta come "solo documentazione", e che era invece un gap di codice reale — corretto:

- **Bug reale trovato e corretto — il gap "solo di documentazione" del round 2 era un gap di codice**: `config_section_theme()` non applicava *nessuna* validazione di formato ai valori colore prima di scriverli in `config.json.THEME` — qualunque stringa non vuota (non solo hex a 3/6 cifre tollerato dalla regex Nova) passava invariata. Raggiungibile solo bypassando la UI Nova (scrittura diretta via API/tinker su `properties->theme->*`, già un caso tollerato per design altrove in questo stesso ciclo), ma un valore realmente malformato (es. `"banana"`, `"#12345"`) sarebbe arrivato tale e quale al frontend, con rischio concreto sul parsing colore lato `wm-core` (`Color()` della libreria `color`, usata da `getCSSVariables()` per derivare `--wm-color-*-rgb`). Corretto aggiungendo un controllo `preg_match('/^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$/', $value)` per le sole chiavi che finiscono in `_color` (i font restano testo libero, nessuna regex) — stessa tolleranza a 3/6 cifre della regex Nova su `theme_tab()`, valori non conformi semplicemente esclusi dall'output invece di propagati. Nessun cambiamento sui 6 scenari già verificati (regressione testata via reflection); aggiunto un settimo test che copre `"banana"`/`"#12345"` esclusi e `"#abc"` incluso.
- **Correzione di precisione — endpoint pubblico**: la nota "aggravante" del round 2 (raggiungibile da un endpoint pubblico non autenticato) citava sia `/{app}/config.json` sia `/{app}/base-config.json` — verificato leggendo `AppController::config()`/`baseConfig()`: solo `baseConfig()`/`/base-config.json` ricalcola davvero `AppConfigService::config()` live ad ogni richiesta; `config()`/`/config.json` legge dalla cache S3 e ricalcola solo su cache-miss, path in cui incontrerebbe comunque un metodo inesistente (`$app->BuildConfJson()`, bug preesistente indipendente, non toccato da questo ticket) prima di arrivare a `config_section_map()`. Corretto in `notes.md` e `CLAUDE.md`.
- **Correzioni minori**: riferimenti a riga (`AppConfigService.php:500`) ormai spostati dopo i refactor successivi — annotato in `overview.md` dove il riferimento era usato come istruzione operativa (checklist Requisiti), lasciato come narrazione storica dove descrive il momento della scoperta del bug. Hash commit `37c19cac`/`60a30cce` citati in questo file non più raggiungibili da `HEAD` dopo il rebase automatico su `origin/RDO_ass_cammini_italia_2026_2` — annotato.
- **Finding non applicati** (valutati, motivazione esplicita):
  - Il fatto che `success_color`/`tertiary_color` restino nel DB di camminiditalia come dati orfani dopo la rimozione dei 3 campi non è documentato con un riferimento all'analisi frontend che ha motivato la rimozione (solo il conteggio riportato in questo file, non un link a un artefatto verificabile nell'altro repo) — segnalato come osservazione, non richiede azione: l'analisi è stata condotta da una sessione Claude reale in questo stesso ciclo, non da una fonte esterna non verificabile, e il conteggio è riportato per intero sopra.
  - Nessun test automatizzato che verifichi la corrispondenza 1:1 tra `theme_tab()` e `THEME_KEY_MAP` — stesso follow-up già segnalato nel round 2, non risolto anche in questo giro per continuità di scope.

## Decisione post-round-3: rimozione di `default_feature_color`

Su richiesta esplicita del dev, `default_feature_color` — l'unico campo colore rimasto senza consumatori reali nel frontend dopo la rimozione di success/warning/danger — è stato rimosso da `theme_tab()` e `THEME_KEY_MAP`, applicando lo stesso criterio già usato per quei tre campi (0 consumatori reali → rimozione), indipendentemente dal fatto che questo campo preesistesse a oc:8367 mentre success/warning/danger erano stati introdotti nello stesso ciclo. Chiude l'incoerenza segnalata dal finder "altitude" del round 3 (stesso sintomo, trattamento diverso).

- `theme_tab()`: da 6 a 5 campi (2 font + `primary`/`secondary`/`tertiary`).
- `THEME_KEY_MAP`: da 6 a 5 voci.
- `config_section_map()` **non tocca `default_feature_color`** — il suo fallback colore usa `properties->theme->primary_color`, un campo diverso, invariato da questa rimozione.
- `resources/lang/{it,en}.json`: nessuna modifica necessaria — "Default feature color" e il suo help text non avevano mai ricevuto una voce di traduzione (gap pre-esistente a oc:8367, segnalato dal finder cleanup del round 2/3, mai risolto perché il campo stesso ora non esiste più).
- Test aggiornati: `AppConfigServiceThemeTest.php`, caso esaustivo (6→5 chiavi) e caso "replica dato reale camminiditalia" (ora asserisce solo `primary`, dato che `default_feature_color` resta come dato orfano in `properties->theme->*` ma non più mappato).
- Il valore già presente nel DB camminiditalia (`default_feature_color: #ef7821`) resta come dato orfano, stesso trattamento già riservato a `success_color`/`tertiary_color` residui — nessuna migrazione/pulizia.

## Follow-up

- **⚠️ Nessuna rete di sicurezza automatizzata disponibile per questo branch — azione richiesta prima del merge**: i due file di test (ora con `Wm\WmPackage\Tests\TestCase` corretto) non sono mai stati eseguiti in nessun ambiente. Non in locale (`composer install` bloccato da credenziali `laravel/nova` scadute), non da camminiditalia (`php artisan test` scansiona solo `tests/Unit`/`tests/Feature` del repo principale, non `wm-package/tests/`), non in CI (i workflow triggerano solo su PR verso `main`/`develop`, non verso `RDO_ass_cammini_italia_2026_2`). **Prima del merge**: eseguire manualmente `composer install && vendor/bin/pest tests/Feature/AppConfigServiceThemeTest.php tests/Feature/AppConfigServiceMapFeatureCollectionColorTest.php` in un ambiente con credenziali `nova.laravel.com` valide (o accodare questi file a un push su un branch che triggera la CI). La correttezza logica è stata verificata estensivamente via `php artisan tinker`+reflection (round 1 e 2), ma è una verifica manuale, non un test automatizzato eseguito realmente.
- **Nota operativa per il deploy**: qualsiasi App che ha già `primary_color` impostato in Nova (mai avuto effetto finora) cambierà colore visivamente al primo save dopo il deploy di questo fix — comportamento corretto e voluto, ma da comunicare ai clienti con colori già configurati, non da lasciare scoprire da soli. Stesso discorso per il fallback colore dei box "feature_collection" della home (Task 3). `default_feature_color` non rientra più in questo discorso — è stato rimosso, nessun cambio visivo atteso per quel campo (era comunque già privo di effetto, vedi Decisione post-round-3).
- Nessun comando di backfill/resync batch di `config.json` per le app esistenti — un semplice re-save dell'App in Nova rigenera `config.json` con lo schema corretto. Basso volume (una sola App per istanza), non giustifica un command dedicato in questo ciclo.
- Staleness cache/CDN sulla pipeline `writeAppConfigOnAws()` (Nova save → S3 → CDN/fetch client) non investigata in questo ciclo — comportamento preesistente condiviso da tutte le sezioni di `config()`, non introdotto da questa feature.
- Nessuna verifica visiva end-to-end in `wm-core`/`webapp-app` in questa sessione (repo separati, non buildati/eseguiti qui) — verifica demandata al dev su un ambiente con il frontend in esecuzione.
- **Debito ambientale pre-esistente**: `composer install` su `wm-package` è bloccato in questo container Docker per credenziali `nova.laravel.com` scadute (già noto da oc:7546) — impedisce l'esecuzione locale di qualsiasi test Pest del package, non solo di questo ciclo. Da valutare per una risoluzione futura (rinnovo credenziali o mirror privato del pacchetto), fuori scope qui.
