> Ticket: oc:8159

# Notes — Tracciamento bacino di utenza per cammino

## Deviazioni dal piano

- **`getRecentUserPositions()` filtrato per prossimità alle EcTrack del layer, non solo a livello di shard.** Il piano originale (Task 7) prevedeva esplicitamente di *non* ripetere il match PostGIS per i punti live sulla mappa, per contenere il costo della query bulk (compromesso documentato in overview.md come rischio accettato). In verifica manuale reale, con un layer in Puglia/Salento, un punto GPS di un utente su un cammino completamente diverso è apparso come marker in mezzo al mare Adriatico, a centinaia di km di distanza — il compromesso era corretto sulla carta ma inaccettabile in pratica per la fiducia nel dato mostrato. Il dev ha chiesto esplicitamente il fix dopo aver visto lo screenshot. Applicato lo stesso filtro `ST_DWithin` già usato da `countPersonsNearLayerTracks()` (nuovo metodo `filterPointsNearLayerTracks()`), con costo aggiuntivo di una query PostGIS bulk per apertura pagina Layer, mitigato dalla cache 90s già esistente.
- **Query HogQL di `getRecentUserPositions()` ora richiede `person_id`** (prima solo `avg(lat)`/`avg(lng)` senza raggruppare per utente in modo esplicito nel `SELECT`) — necessario per costruire i punti da passare al filtro geografico, che richiede un identificativo per persona (non usato per l'output finale, solo per il join PostGIS).
- **Redesign completo dell'aggregazione temporale, sostituita da pre-filtro bbox geografico — deviazione più grande di questo ciclo, richiesta dal dev dopo challenge sul design originale.** Il design iniziale (bucket per persona/ora, `avg(lat)`/`avg(lng)`) è stato messo in discussione dal dev con un'osservazione corretta: la media delle coordinate in un bucket temporale può cadere fuori dal sentiero anche se ogni punto reale era sul sentiero (tipico su tornanti), causando falsi negativi sistematici. Il dev ha inoltre distinto esplicitamente due domande con semantiche diverse, prima trattate con la stessa logica: (1) KPI storico ("ha mai percorso il cammino nel range?") — domanda esistenziale, richiede di testare ogni punto senza aggregare; (2) mappa live ("dove è adesso?") — richiede la posizione più recente reale, non una media. Fix applicato: nuovo helper `layerTracksBoundingBox()` (bbox delle EcTrack del layer via `ST_Extent`, con margine) usato per restringere la query HogQL ai soli punti geograficamente plausibili PRIMA di interrogare PostHog — questo rende possibile testare ogni punto grezzo individualmente per il KPI (nessuna aggregazione, `queryUserMovedPointsNearLayer()` sostituisce `queryUserMovedBucketedPoints()`) senza esplodere il volume, e per la mappa live sostituisce `avg()` con `argMax(valore, timestamp)` (un fix GPS reale, non un punto sintetico) mantenendo il filtro post-bbox `ST_DWithin` già esistente. Finestra della mappa live ridotta da 60 a 30 minuti su richiesta esplicita del dev (concetto di "adesso" più stretto).
- **Bug scoperto durante la verifica con dati reali: `BETWEEN` su un'espressione calcolata rompe HogQL con HTTP 500.** Testando `getUserMovedStats()`/`getRecentUserPositions()` con un layer reale (id 40), entrambi i metodi fallivano silenziosamente (fallback a `null`/`[]`, comportamento corretto ma mascherava il problema). Isolato via chiamate dirette all'API HogQL: `... BETWEEN 42.04 AND 42.36` su `toFloatOrDefault(properties.user_location.latitude, 0.0)` → HTTP 500 (`server_error`, nessun dettaglio ulteriore); la stessa condizione logica scritta come `>= 42.04 AND <= 42.36` → HTTP 200, risultati corretti. Bug/limite del backend HogQL di PostHog, non del codice — confermato con test A/B diretti su API reale prima di modificare il codice. Fix: nuovo helper privato `bboxFilterClause()` che genera sempre `>=`/`<=` incatenati, mai `BETWEEN`, usato da entrambe le query bbox.
- **Estensione di scope, non prevista nel piano originale: ranking "Cammini più frequentati" nell'index Layer.** Richiesta dal dev dopo aver visto la sezione esistente "Cammini più aperti" (oc:8182) sulla card globale e aver chiesto se fosse fattibile un analogo per la nuova metrica. Discussa la differenza di complessità (quel ranking ha una scorciatoia — `layer_id` flat sull'evento `layerOpened` — che `userMoved` non ha) e le due strategie possibili (loop per-layer, scartato per lentezza; query bulk globale con bbox unione di tutte le tracce, scelta). Il dev ha confermato di volerlo comunque in questo ticket ("coerente con la richiesta dell'utente"). Nuovo metodo pubblico `getAllLayersUserPresence()` + due metodi privati (`allTracksBoundingBox()`/`tracksBoundingBox()` refactored da `layerTracksBoundingBox()`, `queryAllUserMovedPointsNearAnyTrack()`, `countPersonsPerLayerNearTracks()`). Verificato con dati reali: 20 layer nel ranking (rispettato `RANKING_LIMIT`), primi risultati coerenti con l'attività osservata nei test precedenti (Cammino del Gran Sasso: 37 utenti su 30gg).

## Bug trovati

- **HogQL: `BETWEEN` su un'espressione calcolata → HTTP 500** — dettaglio completo nella sezione "Deviazioni dal piano" sopra. Bug del backend PostHog, non del codice; fix con comparazioni `>=`/`<=` incatenate.
- **`layerable_type` hardcoded (`'App\Models\EcTrack'`) in `countPersonsPerLayerNearTracks()`, trovato in review-gate.** Il riepilogo di diff isolato (subagente, `execution: review-gate`) ha segnalato che il nuovo metodo del ranking globale usava una stringa letterale per il join sulla pivot `layerables`, invece del pattern dinamico già esistente altrove nel package (`EcPoiEcTrack::poiStillLinkedToLayerViaOtherTrack()`: `config('wm-package.ec_track_model')` + `Relation::morphMap()`). Funzionava correttamente per camminiditalia (verificato con dati reali sia prima che dopo il fix — stesso risultato) solo per coincidenza: `ec_track_model` in questo progetto risolve esattamente a quella stringa. Per un consumer multi-tenant di `wm-package` con una configurazione diversa (es. nessuna sottoclasse `App\Models\EcTrack` locale), il join non avrebbe trovato corrispondenze e il ranking sarebbe stato silenziosamente vuoto — nessun errore, solo un dato sistematicamente sbagliato. Fix: stesso pattern dinamico di `EcPoiEcTrack`, applicato con verifica su dati reali post-fix.

Per il resto, nessun bug nel codice della feature stessa (a parte il redesign descritto sopra, applicato prima di considerare la feature completa). Trovati e documentati (non corretti, fuori scope) diversi gap ambientali preesistenti nella suite di test standalone di `wm-package` in questo ambiente Docker locale:
- Autoload stale (`FieldServiceProvider` non trovato) — risolto con `composer dump-autoload`, non un bug di codice
- `StorageService::getShardName()` ritorna `null` (TypeError) quando `SHARD_NAME`/`APP_NAME` sono assenti nell'ambiente di test — innescato da `AppObserver::created()` ad ogni creazione di `App::factory()`
- Creare un `App` in questo ambiente standalone innesca anche un tentativo di risoluzione disco S3 (region mancante) e un `TypeError` in `Spatie\Permission\PermissionRegistrar` (config permission non mergiata, nessun `PermissionServiceProvider` in `tests/TestCase.php::getPackageProviders()`) — riprodotto identico anche su test esistenti non toccati da questa feature (`AnalyticsControllerLayerAuthorizationTest.php`, `LayerGetFeatureCollectionMapEcPoisTest.php`), confermato con un test A/B (stash/pop delle mie modifiche) che l'errore è preesistente, non causato da oc:8159
- `config('wm-package.ec_track_model')` ha default `'App\Models\EcTrack'` (pensato per i consumer) — non esiste nell'autoload standalone di wm-package, va sempre sovrascritto nei test che toccano `Layer::ecTracks()`/`getFeatureCollectionMap()`
- `AnalyticsControllerUserPresenceTest.php` (nuovo test di questa feature) richiede `App\Models\User`, non risolvibile dal phpunit standalone di wm-package per lo stesso motivo — scritto correttamente ma non eseguibile in locale in questo ambiente; nessun runner alternativo trovato che lo risolva (né `camminiditalia/vendor/bin/phpunit` — la sua `phpunit.xml` non include `wm-package/tests/`, né wm-package/composer.json ha una mappatura `App\` verso `workbench/app/`)

Questi gap sono trasversali alla suite esistente, non introdotti da questa feature — meritano un ticket dedicato se si vuole una suite `wm-package` standalone realmente eseguibile end-to-end (aggiungere `Spatie\Permission\PermissionServiceProvider` e un mock/fake per lo storage S3 a `tests/TestCase.php::getPackageProviders()`, valutare l'attivazione del workbench Testbench per risolvere `App\Models\User` nei test che ne hanno bisogno).

## Decisioni

- **`COUNT(DISTINCT person_id)`, non `distinct_id`** — coerenza con `queryUniqueUsers()` già presente nella stessa card, per non mostrare due KPI con basi di conteggio diverse fianco a fianco (deciso in Fase: challenge, non nell'overview originale del ticket).
- **`shard_name` e sintassi HogQL verificate contro dati reali PostHog prima di scrivere il codice** (Task 0): il ticket assumeva `properties.shard_name` piatto — in realtà è un oggetto `{"_value": "..."}` (super-property registrata via `initAndRegister()` lato mobile, come `app_id`/`app_version`/`app_platform`). Corretto in `properties.shard_name._value`. Scoperta anche l'inesistenza della funzione `toFloat64OrNull()` in HogQL — sostituita con `toFloatOrDefault(valore, default)`.
- **`node_modules` assente per `LayerAnalyticsCard`** (mai installato in questo ambiente) e **dipendenza `laravel-nova-devtool` irrisolvibile** (`file:vendor/laravel/nova-devtool`, pacchetto privato assente — stesso problema già documentato in CLAUDE.md per oc:7546 su un campo diverso). Risolto con workaround temporaneo locale: rimossa la dipendenza da `package.json` solo per l'installazione, pinnato `webpack` a `5.75.0` esatto (stesso fix già documentato per oc:7546 — il range `^5.75.0` risolve a una versione incompatibile con `webpack-cli@4`), eseguito il build, poi ripristinati `package.json`/`package-lock.json` originali. Il dist compilato risultante è corretto e verificato (contiene la stringa "Utenti sul cammino"); nessuna modifica permanente ai file di dipendenze.

## Review formale (`wm-skills:wm-review-ticket`, 5 finder paralleli su `git diff 721e80bd..HEAD`)

Eseguita dopo il review-gate leggero (che aveva già trovato e corretto il morph-type hardcoded). Verdetto: nessun blocker di correttezza sulla logica di business (Finder 1 confermato: il codice risponde bene alla richiesta del cliente), ma trovato un blocker di robustezza e un requisito di accessibilità non rispettato. Corretti entrambi prima di considerare il ciclo chiuso.

**Blocker corretto — catch troppo stretto in `getUserMovedStats()`/`getRecentUserPositions()` (Finder 2, verificato).** Entrambi i metodi catturavano solo `AnalyticsQueryException|LockTimeoutException`, non `\Illuminate\Database\QueryException` — che le nuove query PostGIS bulk (`countPersonsNearLayerTracks()`, `filterPointsNearLayerTracks()`, `layerTracksBoundingBox()`) possono lanciare su un errore Postgres transitorio. `AnalyticsQueryException` estende `Exception` piatta, nessuna relazione con `QueryException` — verificato leggendo `src/Exceptions/AnalyticsQueryException.php`. Senza il fix, un errore DB in `getUserMovedStats()` avrebbe fatto fallire l'intera risposta di `AnalyticsController::layer()` (esattamente il comportamento che il docblock del metodo promette di evitare), e in `getRecentUserPositions()` avrebbe fatto fallire l'intero `getFeatureCollectionMap()` del layer (tracce/POI/taxonomy_wheres compresi, non solo i marker live). Fix: `catch (\Throwable $e)` in entrambi.

**Requisito accessibilità non rispettato — marker distinto solo per colore (Finder 1, verificato leggendo `FeatureCollectionMap.vue`).** `overview.md` richiedeva esplicitamente "forma/icona diversa dai POI... il colore da solo non è sufficiente per l'accessibilità", ma sia il marker live sia gli EcPoi erano cerchi pieni semplici (`CircleStyle`), distinti solo da colore e un piccolo delta di raggio — nessuna differenza di forma. Fix: aggiunta `checkpointRouteColors` (già supportata da `FeatureCollectionMap.vue::getFeatureStyle()` per un altro caso d'uso, percorsi multi-tratta — nessuna modifica al componente Vue necessaria) per un profilo ad anelli concentrici (bullseye) sul marker live, genuinamente distinguibile per forma da un EcPoi anche in scala di grigi.

**Cleanup corretti:**
- Cache key di `getRecentUserPositions()` non includeva `$minutesWindow` — bug latente (oggi inerte, un solo chiamante usa sempre il default), corretto aggiungendo il parametro alla chiave.
- `getRecentUserPositions()` non loggava un `Log::warning` al raggiungimento del cap `LIMIT 500`, a differenza di tutti gli altri metodi con cap di sicurezza in questa classe — aggiunta nuova costante `MAX_RECENT_POSITIONS` + log, per consistenza.
- Legenda "Posizione utente" in `DetailField.vue` veniva mostrata su **tutte** le risorse che usano il campo `FeatureCollectionMap` (EcTrack, TaxonomyWhere, Media), non solo su Layer — l'unica che può davvero avere marker live. Corretto con `v-if="resourceName === 'layers'"`.
- `overview.md` "Moduli toccati" elencava `LayerAnalyticsCard.php` (mai realmente modificato) e non elencava `AnalyticsController.php`/`DetailField.vue` (entrambi modificati) — corretto l'elenco.
- Task 3 Step 5 del piano (verifica `EXPLAIN ANALYZE` + follow-up se manca un indice GIST) non era mai stato registrato in questo file. Verificato ora: `ec_tracks.geometry` ha già un `spatialIndex` (GIST) via `database/migrations/create_ec_tracks_table.php.stub` — nessun follow-up necessario, il controllo era stato fatto (vedi Task 3 nel piano) ma la sua conferma non era stata scritta qui.

**Debito accettato, non corretto in questo ciclo** (segnalato da Finder 4 e Finder 5, entrambi livello cleanup/design, non bloccanti):
- Duplicazione strutturale tra `queryUserMovedPointsNearLayer()`/`queryAllUserMovedPointsNearAnyTrack()` (quasi identici, solo bbox/cap/log differiscono) e tra `countPersonsNearLayerTracks()`/`filterPointsNearLayerTracks()` (stessa query VALUES+EXISTS+ST_DWithin, solo la SELECT esterna cambia) — una funzione condivisa per la costruzione della clausola `VALUES` + bindings (ripetuta identica in 3 metodi) risparmierebbe ~24 righe duplicate.
- `AnalyticsService` ha superato le 1000 righe con questo ticket (+527, da ~646 a 1173) e ora mescola due responsabilità distinte: client HogQL/PostHog e matcher geografico bulk PostGIS — Finder 5 suggerisce l'estrazione di un `GeoMatchingService` dedicato, che renderebbe anche testabile la logica di match senza dover passare da `Http::fake()`.
- `LIMIT 500` in `getRecentUserPositions()` era un magic number senza costante dedicata (ora corretto, vedi sopra) ma resta il pattern generale: 4 varianti quasi-duplicate di query punti/match sono un'occasione di unificazione persa, non un errore.

Nessuna azione presa su questi tre punti in questo ciclo — sono debito tecnico consapevole, non requisiti del ticket né bug. Da valutare in un ticket dedicato se il pattern (bbox + match PostGIS bulk) viene riusato da una quinta feature.

## Follow-up round 2 — dedup query + nominativo/link cliccabile sul marker live

Richiesto dal dev dopo la chiusura del ciclo precedente, in due passaggi: (1) eliminare la duplicazione tra le 3 varianti di query HogQL e le 3 varianti di match PostGIS bulk segnalata come debito accettato dal Finder 4/5 della review formale (vedi sopra); (2) mostrare nominativo e link cliccabile sul marker live quando l'evento `userMoved` porta `properties.user_id`.

**Dedup (1) — solo codice, nessuna esecuzione/cache toccata.** Estratti `pointsValuesClause()` (condiviso da `countPersonsNearLayerTracks()`, `countPersonsPerLayerNearTracks()`, `filterPointsNearLayerTracks()`) e `fetchUserMovedPointsRows()`/`mapUserMovedRows()` (condivisi dai 3 fetch HogQL). Deciso esplicitamente di **non** unificare le due *esecuzioni* KPI/mappa-live (TTL cache incompatibili: 15min-6h contro 90s fisso) né di derivare `countPersonsNearLayerTracks()` da `filterPointsNearLayerTracks()` (nessun beneficio sulla latenza, il costo dominante è il round-trip HogQL non la forma della query Postgres) — entrambe le opzioni scartate su richiesta del dev dopo aver chiarito che l'obiettivo era il tempo di attesa, non la sola pulizia del codice.

**Nominativo/link (2).** `user_id` (rinominato da `idUser` su richiesta del dev, coerente con la convenzione snake_case già usata per `person_id`/`layer_id`/`track_id` in questa stessa classe) viene recuperato solo dalla query della mappa live (`argMax(toInt64OrNull(properties.user_id), timestamp)`), propagato attraverso `filterPointsNearLayerTracks()` (chiave di join `person_id`) e risolto in `Layer::getFeatureCollectionMap()` via batch-lookup su `Wm\WmPackage\Models\User` (non `App\Models\User`, per restare autoloadabile nella suite standalone di wm-package — stessa tabella, nessuna differenza di dati). Verificato che `id` è già presente nella risposta `login`/`me` di `AppAuthController` (non è tra i campi `$hidden`), quindi il mobile può già inviarlo a PostHog senza modifiche lato backend.

**Review (3 agenti paralleli sul diff non commitato) — 3 bug reali trovati e corretti prima del commit:**
- **Query User fuori dal try/catch di `getRecentUserPositions()`** — quel catch esiste esplicitamente (commento a riga 313-318 di `AnalyticsService.php`) per evitare che un errore DB rompa l'intero `getFeatureCollectionMap()`; il nuovo lookup batch in `Layer.php` girava fuori da quella protezione. Fix: try/catch dedicato attorno al lookup, fallback a `collect()` vuota su errore (stesso principio: l'arricchimento è cosmetico, non deve mai rompere la mappa).
- **Link e tooltip erroneamente accoppiati sulla stessa condizione** (`$nominativo !== ''`) — uno `User` risolto con `name`/`surname` vuoti perdeva sia il nominativo (corretto) sia il link cliccabile (non dovrebbe: lo user esiste ed è comunque identificabile). Fix: il link è gated su `$user` (risolto o no), il tooltip resta gated sul nominativo non vuoto. Nuovo test dedicato (`test_position_keeps_link_when_user_is_found_but_nominativo_is_blank`).
- **`toInt64OrNull(properties.user_id)` non verificabile contro dati reali** — a differenza di ogni altra funzione/property HogQL introdotta in questo ticket (sempre verificata via query dirette contro l'API PostHog reale, vedi Task 0 sopra), qui non è possibile: la property non esiste ancora in produzione. Non un bug da correggere ora — documentato come rischio esplicito in overview.md, con l'azione di verifica rimandata a quando il team mobile inizia a inviarla (vedi Follow-up sotto).

**Decisione privacy, presa dal dev dopo verifica congiunta**: `LayerPolicy::view()` non fa scoping per ownership (solo l'indice Nova è filtrato per i non-Administrator, via `indexQuery()` — confermato anche da `LayerPolicyTest::test_validator_can_view_any_layer`, un test esistente che verifica esplicitamente che un Validator possa aprire un layer non suo). Il dev ha valutato la cosa e scelto di procedere senza gate aggiuntivo (nessuna restrizione Administrator-only sul nominativo/link), accettando lo stesso livello di esposizione già presente per le altre informazioni di quella pagina (tracce, POI). Dettaglio completo in overview.md → "Estensione: nominativo e link cliccabile sul marker live".

**Cleanup fatto in review**: estratto un helper privato `createLayerWithTrack()` nel file di test (il blocco `Model::withoutEvents(fn () => App+EcTrack+Layer+attach)` era duplicato 4 volte su 5 test dopo l'aggiunta dei nuovi casi).

## Follow-up

- Calibrazione di `LAYER_USER_PRESENCE_DISTANCE_METERS` (default 50m) da rivedere dopo un periodo di osservazione in produzione — nessun dato storico per validarla a priori (già segnalato come rischio in overview.md).
- **Verificare `toInt64OrNull(properties.user_id)` con una query reale non appena il team mobile inizia a inviare `user_id`** (round 2) — se la funzione non esiste in HogQL o la property arriva incapsulata come `{"_value": ...}`, la feature nominativo/link non si attiverà mai, senza errore visibile.
- Estrazione di un `GeoMatchingService` dedicato (Finder 5) e consolidamento delle 3 varianti di query VALUES-based (Finder 4) — debito tecnico accettato, vedi sopra (il consolidamento query è stato in parte fatto nel round 2, l'estrazione del service dedicato resta aperta).
- Suite di test standalone `wm-package` (vedi "Bug trovati" sopra): gap ambientali preesistenti da affrontare in un ticket dedicato, non in questo ciclo.
- Bug cross-tenant `shard_name` sugli altri metodi di `AnalyticsService`: tracciato separatamente in **oc:8354**.
